class Solution {
public:
    vector<string> summaryRanges(vector<int>& nums) {
        int start = nums[0];
        int current = nums[0];
        int end;

        for (int i = 0; i < nums.size(); ++i){
            
        }
    }
};
