class Solution {
public:
    int removeDuplicates(vector<int>& nums) {

        // start at the second index
        // check if the index is equal to the previous index
        // if it is not, we have a unique digit so ... hmm

        for (int i = 0; i < nums.size(); i ++) {
            
        }
    }
};