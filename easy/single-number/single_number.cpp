#include <algorithm>
class Solution {
public:
    int singleNumber(vector<int>& nums) {
        int candidate = nums[0];
        for (int i = 1; i < nums.size(); ++i) {
            if 
        }
    }
};
