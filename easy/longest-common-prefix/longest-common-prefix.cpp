class Solution {
public:
    string longestCommonPrefix(vector<string>& strs) {
        bool same_letter_check = true;
        for (int i = 0; i < strs.size(); i ++) {
            std::string prefix = "";
            for (int j = 0; j < strs.size(); j ++) {
                if 
            }
        }
    }
};